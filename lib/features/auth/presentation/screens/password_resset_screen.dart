import 'package:flutter/material.dart';

class PasswordRessetScreen extends StatelessWidget {
  const PasswordRessetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

body: Center(child: Text("this is passwored resset screen")),



    );
  }
}